# 운영 배포 환경변수 (docker-compose.prod.yml 용)
# 사용: cp .env.prod.example .env

# 서비스 도메인 (Caddy 자동 HTTPS 발급 대상). DNS A 레코드가 이 서버를 가리켜야 한다.
DOMAIN=example.com

# Let's Encrypt 인증서 만료 알림용 이메일
ACME_EMAIL=admin@example.com
